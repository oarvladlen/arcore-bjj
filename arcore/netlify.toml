[build]
  publish = "."
  command = "node scripts/inject-config.mjs"

[build.environment]
  NODE_VERSION = "20"

# SPA: magic-link callback e rotas client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
